---
import { contactDetails } from "../../constants/site";
---

<footer class="bg-[var(--color-primary)] text-white">
  <div
    class="container-shell grid gap-10 py-10 px-8 sm:py-12 lg:grid-cols-[1fr_auto] lg:items-start"
  >
    <div class="space-y-8">
      <div
        class="grid grid-cols-2 gap-8 text-[clamp(1rem,1.35vw,1.25rem)] font-semibold"
      >
        <ul class="space-y-2">
          <li>
            <a href="#nosotros" class="transition-opacity hover:opacity-80"
              >Nosotros</a
            >
          </li>
          <li>
            <a href="#servicios" class="transition-opacity hover:opacity-80"
              >Servicios</a
            >
          </li>
        </ul>
        <ul class="space-y-2">
          <li>
            <a href="#proyectos" class="transition-opacity hover:opacity-80"
              >Proyectos</a
            >
          </li>
          <li>
            <a href="#contacto" class="transition-opacity hover:opacity-80"
              >Contacto</a
            >
          </li>
        </ul>
      </div>

      <div class="space-y-1 text-sm text-white/90">
        <p>Terminos y condiciones</p>
        <p>Aviso de privacidad</p>
      </div>

      <div class="space-y-1 text-sm text-white/90">
        <p>{contactDetails.handle}</p>
        <p>{contactDetails.email}</p>
        {contactDetails.phones.map((phone: string) => <p>{phone}</p>)}
        <a
          href="https://instagram.com/placeholder"
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Instagram de NEO LUM"
          class="inline-flex items-center gap-2 pt-2 text-white transition-opacity hover:opacity-80"
        >
          <svg
            viewBox="0 0 24 24"
            aria-hidden="true"
            class="size-6"
            fill="none"
            stroke="currentColor"
            stroke-width="1.8"
          >
            <rect x="3.5" y="3.5" width="17" height="17" rx="5" />
            <circle cx="12" cy="12" r="4.2" />
            <circle cx="17.3" cy="6.7" r="1.1" fill="currentColor" stroke="none" />
          </svg>
          <span class="text-sm font-semibold">Instagram</span>
        </a>
      </div>
    </div>

    <a
      href="#top"
      aria-label="Volver arriba"
      class="inline-flex justify-start lg:justify-end"
    >
      <img
        src="/assets/LOGO_NEOLUM_PRINCIPAL.svg"
        alt="Logo NEO LUM"
        class="h-auto w-[24rem] max-w-full sm:w-[32rem] lg:w-[50rem]"
        loading="lazy"
      />
    </a>
  </div>
</footer>

<div class="bg-white py-2">
  <div class="container-shell text-center">
    <p class="text-4 text-[var(--color-text)]">
      © 2026 All rights reserved to NEO LUM.
    </p>
  </div>
</div>
